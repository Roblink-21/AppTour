import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tecmov.wtour',
  appName: 'WTour',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
